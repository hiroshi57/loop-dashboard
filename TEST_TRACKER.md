# DI Loop Library — ユーザーストーリー & テスト追跡シート

> 生成日: 2026-06-30 | 対象: index.html (loop-dashboard)
> ステータス凡例: ✅ PASS | ❌ FAIL | ⚠️ 部分的 | 🔲 未テスト

---

## SECTION A: 初期表示・データ読み込み

| # | ユーザーストーリー | 期待動作（コードより） | ステータス | バグ/備考 |
|---|---|---|---|---|
| A-1 | ユーザーがサイトを開く | `load()` が `loops_1000.json` を fetch し、KPIカード（1,126 / 13 / 10 / 15）がヘッダーと画面上部に表示される | 🔲 | |
| A-2 | JSONの読み込みに失敗した場合 | `#grid` に「loops_1000.json を読み込めませんでした」エラーメッセージが表示される | 🔲 | |
| A-3 | ページ初回ロード時にダークモード設定が反映される | `localStorage["di-theme"]` → OS設定 の順で取得し `html[data-theme]` に適用される | 🔲 | |
| A-4 | ヘッダーに総数が表示される | `#hdCount` に「**1,126** loops / **13** services / **10** industries」が表示される | 🔲 | |
| A-5 | フッターにメタ情報が表示される | `#footMeta` に生成日・ループ数・典拠・機密レベルが表示される | 🔲 | |

---

## SECTION B: チャート・ビジュアライゼーション

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| B-1 | OVERVIEWセクションで棒グラフを見る | デフォルト「サービス別ループ数」の横棒グラフが表示される（Chart.js） | 🔲 | |
| B-2 | 「業種別」タブをクリックする | 棒グラフが業種別に切り替わり、タイトルが「業種別ループ数」になる | 🔲 | |
| B-3 | 棒グラフのバーをクリックして絞り込む | `setFilter(axis, key)` が呼ばれ、カード一覧が絞り込まれる。バーがハイライト状態になる | 🔲 | |
| B-4 | 棒グラフの絞り込み済みバーを再クリックする | フィルタが解除（`"all"` に戻る）され全件表示になる | 🔲 | |
| B-5 | ドーナツチャートでカテゴリ構成比を見る | 6カテゴリ（戦略立案/調査・分析/制作・実装/運用・最適化/品質・コンプラ/レポート・報告）が各色で表示される | 🔲 | |
| B-6 | ドーナツチャートのセグメントをクリックする | カテゴリフィルタが適用され、カード一覧がそのカテゴリのみに絞り込まれる | 🔲 | |
| B-7 | トリガー内訳バーを見る | manual/interval それぞれの件数と割合がバーで表示される | 🔲 | |
| B-8 | テーマを切り替える | チャートが `rebuildCharts()` で再描画され、ダーク/ライトの配色が変わる | 🔲 | |

---

## SECTION C: 課題（Pain）セレクター

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| C-1 | 「課題からループを探す」で課題カードを見る | 15種の課題カードが件数付きで表示される | 🔲 | |
| C-2 | 課題カードを1つクリックする | カードが `.on` 状態になり、`state.pains` に追加、絞り込みが適用され `#browse` へスクロール | 🔲 | |
| C-3 | 選択済みの課題カードを再クリックする | `state.pains` から除去され、カードが `.on` から外れる | 🔲 | |
| C-4 | 課題カードを2つ以上選択する | OR検索で合計件数が表示され、`#painGrid` 下に「N件選択中（OR）」バッジが表示される | 🔲 | |
| C-5 | 課題フィルタが適用された状態でもう一度同じ課題をクリックする | 解除される（配列から splice） | 🔲 | |

---

## SECTION D: 検索・フィルタ

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| D-1 | フリーワードを入力する | 150ms debounce後に `apply()` が実行、タイトル/Goal/戦略/コンテキスト/タグ/業種/サービスから検索 | 🔲 | |
| D-2 | 日本語カッコ付きで検索する（例:「スパム」） | `expandQuery()` がカッコを除去し `スパム` として検索、正しくヒット | ✅ | 2026-06-30修正済み |
| D-3 | シノニムで検索する（例:「SEO」→「検索上位」） | `SYNONYMS` マップで展開され、関連ループがヒット | 🔲 | |
| D-4 | ✕ボタンで検索をクリアする | 入力欄が空になり `state.q=""` で全件表示に戻る | 🔲 | |
| D-5 | サービスで絞り込む（タブ切替→ドロップダウン選択） | `filterAxis="service"` でドロップダウンにサービス一覧が表示され、選択で絞り込み | 🔲 | |
| D-6 | 業種で絞り込む | `filterAxis="industry"` でドロップダウンに業種一覧、選択で絞り込み | 🔲 | |
| D-7 | 課題で絞る（フィルタタブ） | `filterAxis="pain"` でドロップダウンに課題一覧、選択で絞り込み（pains配列で管理） | 🔲 | |
| D-8 | カテゴリチップをクリックする | `state.category` が更新され絞り込み適用、チップが `.on` 状態になる | 🔲 | |
| D-9 | トリガーチップをクリックする | `state.trigger` が更新され絞り込み適用 | 🔲 | |
| D-10 | 同じカテゴリチップを再クリックする | `state.category="all"` にリセット（トグル動作） | 🔲 | |
| D-11 | 複数フィルタを同時に適用する | AND条件で絞り込まれ、アクティブピルが複数表示される | 🔲 | |
| D-12 | アクティブピルの「✕」をクリックする | 対応するフィルタだけが解除され残りは維持 | 🔲 | |
| D-13 | 「すべて解除」ボタンをクリックする | 全フィルタ・検索がリセットされ全1,126件表示 | 🔲 | |
| D-14 | 該当ゼロの状態になる | 「該当するループがありません」の空状態メッセージが表示される | 🔲 | |

---

## SECTION E: カード一覧・ページネーション

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| E-1 | カード一覧を見る | 1ページ24件表示（`PER_PAGE=24`）、バッジ/タイトル/goal/戦略/イテレーションリング/KPIタグ/課題タグ/フッターが表示 | 🔲 | |
| E-2 | カードにホバーする | `translateY(-2px)` で浮き上がり、border-colorが変わる | 🔲 | |
| E-3 | ページネーションで次のページへ移動する | 2ページ目を表示し、`#browse` へスクロール | 🔲 | |
| E-4 | ページネーションの最初/最後ページで矢印ボタンが無効になる | 先頭では `‹` が disabled、最後では `›` が disabled | 🔲 | |
| E-5 | ページ情報（N–M / 総件数）が表示される | `pinfo` に「1–24 / 1,126 loops」形式で表示 | 🔲 | |
| E-6 | 絞り込み後に24件以下になるとページネーションが消える | `filtered.length <= PER_PAGE` のとき `#pager` が空になる | 🔲 | |

---

## SECTION F: ループ詳細モーダル

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| F-1 | 「詳細」ボタンをクリックする | モーダルが `fadeUp` アニメーションで開き、ループタイトルとバッジが表示される | 🔲 | |
| F-2 | モーダル上部「このループがすること」を見る | `goal` が太字1行で表示される（`.tldr`） | 🔲 | |
| F-3 | 「いつ動く（始まり方）」を見る | トリガーチップ（🖐手動/⏱定期）＋`cleanTriggerDetail()` 済みの説明文が表示される | 🔲 | |
| F-4 | 「完了の合図」「くり返し」を見る | `exitCondition` と `maxIterations` が表示される | 🔲 | |
| F-5 | 「この業種での効かせ方」を見る | `context`/`strategy`/`guardrails` が緑左ボーダーのボックスで表示される | 🔲 | |
| F-6 | 業種の異なる同アーキタイプのループを比較する | 「前提」「狙い」「守ること」が業種ごとに異なる内容で表示される（医療≠旅行） | ✅ | 2026-06-30修正済み |
| F-7 | サイクル早見マップを見る | `1 計画（まず狙いを決める）→ 2 実行（小さく試す）→ … ↺ 1へ戻る` が表示される | 🔲 | |
| F-8 | 流れ（5ステップ）を見る | STEP1〜5が番号付きで縦に並び、`↓` でつながり、末尾に `↺` ループの締めが表示される | 🔲 | |
| F-9 | 「もっと詳しく見る」を開く | `<details>` が展開し、6要素/コアモジュール/方法論/戦略補足/ループ定義/運用ルール/継続運用/成果物KPI/課題/KICKOFFプロンプトが表示される | 🔲 | |
| F-10 | モーダルを「✕」ボタンで閉じる | `closeModal()` が実行されオーバーレイが消え、`body` スクロールが復帰する | 🔲 | |
| F-11 | モーダル外（オーバーレイ）をクリックして閉じる | `e.target === overlay` の判定で `closeModal()` が実行される | 🔲 | |
| F-12 | Escキーでモーダルを閉じる | `keydown` リスナーが `e.key==="Escape"` を検知し `closeModal()` を実行 | 🔲 | |
| F-13 | モーダル内の「Copy」ボタンを押す | `kickoffPrompt` がクリップボードにコピーされ、トーストが表示される | 🔲 | |

---

## SECTION G: コピー機能

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| G-1 | カードの「Copy」ボタンを押す | `kickoffPrompt` がコピーされ、ボタンが「Copied!」表示に変わり（1400ms後復帰）、トーストが出る | 🔲 | |
| G-2 | クリップボードAPIが使えない環境でCopyする | `execCommand("copy")` のフォールバックが動作する | 🔲 | |
| G-3 | コピー失敗時 | 「コピーに失敗しました」トーストが表示される | 🔲 | |

---

## SECTION H: お気に入り機能

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| H-1 | カードの「★」ボタンを押す | `favorites` Setに追加され、ボタンが `.on`（黄色）になり、お気に入りバーが下部に出現する | 🔲 | |
| H-2 | 「★」を再度押す | `favorites` から削除、ボタンが `.on` 解除、バーの件数が減る（0件でバー非表示） | 🔲 | |
| H-3 | お気に入りバー「まとめてコピー（提案書用）」を押す | 選択中の全ループをMarkdown形式（タイトル/業種/サービス/Goal/Strategy/MAX ITER/EXIT WHEN/参考値/KICKOFFプロンプト）に整形しコピー | 🔲 | |
| H-4 | お気に入りバー「すべて解除」を押す | `favorites.clear()` でリセット、グリッド再描画、バー非表示 | 🔲 | |
| H-5 | お気に入りが0件の状態で「まとめてコピー」を押す | 「お気に入りがありません」トーストが出る | 🔲 | |

---

## SECTION I: パーマリンク（URLハッシュ）

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| I-1 | フィルタを適用するとURLが更新される | `pushHash()` が `history.replaceState` でURLに `#q=...&svc=...` 等を反映 | 🔲 | |
| I-2 | フィルタ付きURLを直接開く | `parseHash()` が実行され、URLのパラメータからフィルタ状態が復元される | 🔲 | |
| I-3 | 複数パラメータ（q + svc + cat）付きURLを開く | 全パラメータが正しく復元され適切に絞り込まれる | 🔲 | |
| I-4 | 全解除するとURLがクリーンになる | ハッシュが消え `location.pathname` のみになる | 🔲 | |

---

## SECTION J: テーマ切替

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| J-1 | 「◐」ボタンをクリックする | `html[data-theme]` が light/dark でトグルし、CSS変数が切り替わり全体の配色が変わる | 🔲 | |
| J-2 | テーマがlocalStorageに保存される | 次回ロード時に同じテーマが維持される | 🔲 | |
| J-3 | OS側がダークモードの場合 | `localStorage` 未設定時は `prefers-color-scheme` を参照しダークで開く | 🔲 | |

---

## SECTION K: 軸詳細パネル（axis-detail）

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| K-1 | サービスを1つに絞り込む | `#axisDetail` が表示され、サービス名/説明/ループ数/カテゴリ別件数チップが表示される | 🔲 | |
| K-2 | 業種を1つに絞り込む | 同様に業種の詳細パネルが表示される | 🔲 | |
| K-3 | 課題を1つだけ選択する | 課題名と「に効くループ」のパネルが表示される | 🔲 | |
| K-4 | 複数選択・全件表示の場合 | `#axisDetail` が非表示になる | 🔲 | |

---

## SECTION L: アクセシビリティ・レスポンシブ

| # | ユーザーストーリー | 期待動作 | ステータス | バグ/備考 |
|---|---|---|---|---|
| L-1 | キーボードでTabキーナビゲーションする | `:focus-visible` で2px primaryアウトラインが表示される | 🔲 | |
| L-2 | `prefers-reduced-motion` が有効な環境 | `animation:none!important` / `transition:none!important` が適用される | 🔲 | |
| L-3 | 980px以下のウィンドウ幅 | カード2列/チャート1列/KPI2列になる | 🔲 | |
| L-4 | 620px以下のウィンドウ幅 | カード1列/`.hd-count`非表示/f-label 100%幅 | 🔲 | |
| L-5 | モーダルに `role="dialog"` `aria-modal="true"` がある | スクリーンリーダーが適切にダイアログを認識できる | 🔲 | |

---

## バグ一覧（テスト後に記入）

| # | 発見箇所 | 内容 | 優先度 | 修正ステータス |
|---|---|---|---|---|
| BUG-01 | D-2 | 「スパム」検索で日本語カッコが除去されずヒットしない | High | ✅ 修正済み（2026-06-30） |
| BUG-02 | F-6 | 業種固有フィールドが折りたたみに隠れ全業種で同じに見える | High | ✅ 修正済み（2026-06-30） |

---

## UX監査バグ一覧（修正済み）

| # | 発見箇所 | 内容 | 優先度 | 修正ステータス |
|---|---|---|---|---|
| UX-01 | TRIGGERチップ | 絞り込みチップが「manual」「interval」と英語のまま（カードバッジは日本語化済みで不統一） | Medium | ✅ 修正済み（2026-06-30）|
| UX-03 | お気に入り | リロードでお気に入りが消える（localStorageへの永続化なし） | Low | ✅ 修正済み（2026-06-30）|
| UX-08 | すべて解除 | 「すべて解除」後も filterAxis が残りドロップダウン表示が古い軸のまま | Low | ✅ 修正済み（2026-06-30）|

## テスト実行ログ

| 日付 | フェーズ | 実施内容 | 結果 |
|---|---|---|---|
| 2026-06-30 | Phase 1 | ユーザーストーリー作成（65件）・コードベース分析 | 完了 |
| 2026-06-30 | Phase 2 | 自動テストスイート実行（_test.py） | **65 PASS / 0 FAIL** |
| 2026-06-30 | Phase 2 | UX監査実行（_ux_audit.py） | **3件検出** |
| 2026-06-30 | Phase 3 | UX-01/03/08 修正 | 完了 |
| 2026-06-30 | Phase 4 | 再テスト（_test.py + _ux_audit.py） | **65 PASS / 0 issues** |

